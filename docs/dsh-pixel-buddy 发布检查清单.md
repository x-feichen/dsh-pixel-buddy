# dsh-pixel-buddy 发布检查清单（T3.1 就绪交接）

| 项目 | 内容 |
|---|---|
| 文档版本 | v1.0 |
| 编制日期 | 2026-09-05 |
| 对应任务 | 任务规划文档 T3.1（发布上线，P1）——依赖 DSH 分发流程与真实宿主环境，当前无法执行，本清单为就绪交接 |
| 使用方式 | DSH 插件文档/环境到位后，逐项执行并勾选；全部通过后按分发流程上线 |

---

## 1. 发布前置（阻塞项，未完成不得上线）

- [ ] **美术资产替换**：按《美术资产需求单》接收设计产出（本体三档 + 四徽章 + 色板规范），替换 `src/buddy/pixel-buddy.element.ts` 的 `placeholderPetSvg()` 与 `badges.ts` 字形，跑全量测试 + devhost 视觉回归
- [ ] **Hook API 核实回填**：按《Hook API 对齐附录》§5 清单逐条核实（事件名 #1、"需要人工"事件 #2、会话上下文 #3 等），修订 `EVENT_MAP` 与 `getDshHookApi()` 真实获取路径
- [ ] **消除途径确认**：D4 若"需要人工"事件不存在，执行 4 态裁剪预案（PRD/澄清结论已备书面方案）
- [ ] **PRD 回填**：动效参数（已定稿 150ms/2.5s）、最终尺寸档位、附录附录编号——PRD 修订版归档

## 2. 真机兼容回归（M2 报告 §3 清单的执行）

- [ ] Chrome / Edge / Firefox / Safari 14+：徽章四态渲染、crossfade、点击消除、主题切换、静止态 Performance 无 rAF
- [ ] 右下角与 DSH Web 真实悬浮元素（toast/客服入口）共存实测；冲突则按共存检测告警信息上报产品裁决错位方案

## 3. 真机性能录制（验收 8 补充证据）

- [ ] DevTools Performance 录制静止态 10s：宠物相关零 rAF 活动、零定时器
- [ ] 内存对比：插件挂载前后 heap snapshot 差值记录（预期 < 1MB）

## 4. 发布执行

- [ ] 版本号确认（当前 0.1.0，建议首发 1.0.0 或按 DSH 规范）
- [ ] `npm run build` 产物 `dist/dsh-pixel-buddy.iife.js`（gzip 5.18KB）通过分发流程打包
- [ ] 灰度方案：建议先按 DSH 插件系统的灰度机制放量（比例依平台能力）
- [ ] **回滚方案确认**：卸载路径验证——`window.dshPixelBuddy.destroy()` + 移除 script 注入，页面无残留节点/定时器/监听（测试已覆盖 destroy 语义）
- [ ] 线上验收：安装→四态流转→点击消除→卸载，全链路通过

## 5. 已知遗留（不阻塞，上线后跟进）

| 事项 | 跟进方式 |
|---|---|
| "需要人工"状态依赖核实清单 #2 | 若事件缺失按预案裁剪为 4 态 |
| 徽章像素字形为占位 | 随美术资产替换 |
| 多会话聚合 | 见《后续版本技术预留评审》§3，需核实清单 #3 |
